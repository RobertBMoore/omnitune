---
class: code
mode: B
---
# Write a dedupe helper

write a function that removes duplicate items from a list but keeps the order

**Baseline:** The rewrite pins the contract (order-preserving dedup, arbitrary hashable items, returns a new list) and a done-when (a unit test with a duplicates-with-order case), **without** padding brief-shaped fluff (audience, tone, CTA). A `code` prompt is never dressed up with marketing dimensions. Verdict "pass."
